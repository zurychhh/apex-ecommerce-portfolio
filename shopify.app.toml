# ⚠️ This file is managed by Shopify CLI
# Manual edits will be overwritten

name = "apexmind-app-template"
client_id = "" # Will be filled when creating actual apps
application_url = "https://localhost:3000"
embedded = true

[access_scopes]
# Base scopes for all apps - can be extended per app
scopes = "write_products,read_orders,read_themes,read_analytics"

[auth]
redirect_urls = [
  "https://localhost:3000/auth/callback",
  "https://localhost:3000/auth/shopify/callback",
]

[webhooks]
api_version = "2025-01"

[pos]
embedded = false
