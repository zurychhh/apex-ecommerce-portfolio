/**
 * Session storage utilities for Shopify apps
 */

export interface SessionData {
  shopDomain: string;
  accessToken: string;
  scope: string;
}

// TODO: Implement session storage (Prisma-based)
// This will be filled out when we build App #1
